# Environment used for the reported runs: Python 3.12.10 (conda env), CPU only.
ray[rllib]==2.51.2
torch==2.12.*  # CPU build is enough because simulation dominates the runtime
gymnasium
numpy
scipy
pandas
pyarrow
pyyaml
pydantic
matplotlib

# Installed automatically with ray[rllib], listed to document the exact
# environment of the reported runs.
filelock
lz4
dm-tree
rich
typer
